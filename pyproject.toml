[project]
name = "omniagents-harness"
version = "0.1.0"
description = "Example OmniAgents configurations — from single-tool assistants to multi-agent supervisors."
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
    "pandas",
    "mlflow",
    "numpy",
    "openai",
]
